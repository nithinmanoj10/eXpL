decl
    int sum, *sumPtr;
    int age, *agePtr;
    str msg, *msgPtr;
enddecl

begin
    read(sum);
    read(age);
    read(msg);

    agePtr = &age;
    sumPtr = &sum;
    msgPtr = &msg;

    write(*agePtr + *sumPtr);
end;