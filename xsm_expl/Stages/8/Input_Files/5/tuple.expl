decl
    int a;
enddecl

int main() {
    decl
        int temp;
        tuple student(str name, int age, int rollNo) studOne, studTwo;
        tuple teacher(str name, str subject, int teacherID) teacherOne;
    enddecl

    begin
        read(studOne.age);
        write("Tuples");
        return 1;
    end;
}