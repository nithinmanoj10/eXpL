decl
	int num, sum, age;
	str msg;
enddecl

begin
	read(num);
	sum = 0;

	while (num != 0) do
		sum = sum + num;
		read(num);
		breakpoint;
	endwhile;
	
	// msg = "Hello World";
	msg = 1;

	write(sum);
	age = 20;
end;
